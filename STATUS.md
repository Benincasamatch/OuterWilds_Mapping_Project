# 当前状态 · 2026-09-19

**0.2.1-dev：可本地预览的预发布版，尚未满足全部原始验收条件，未上传 Workshop。**

## 已完成

- 数据/轨道、程序化 3D 画面、事件时间线、1360 秒循环与接缝效果。
- 2026-09-19 下午视觉迭代：银河带星空与尘埃精灵、太阳米粒对流与色球层、包裹式光照与彩色大气壳、轨道尾迹渐变、超新星核心坍缩与激波环、MSAA 抗锯齿；每帧 draw call 14–21，仍在预算内。
- WE 属性映射与校验、全局帧率、暂停恢复、高 DPI、图形上下文恢复。
- 右下角 + / − / ⌂ 备用缩放和复位按钮，可在属性面板隐藏；不依赖滚轮和键盘。
- 中英文说明、实际渲染封面、白名单交付包与 SHA-256 清单。
- 项目忽略规则已允许源码/测试/工具纳入版本管理；未自动提交。
- 单元回归 **86/86** 项通过。**13/13 项真实浏览器检查通过**（RTX 4070 真实 GPU，Edge 内核），覆盖 DPI、鼠标、按钮、回调、恢复、探针、离线交付包与封面生成；测试入口为源项目 tools/check-browser.mjs。SwiftShader 软件渲染下 11 项核心检查同样通过。

## 真实桌面实测

| 项目 | 结果 |
|---|---|
| 横向桌面拖动 | **通过**，人工操作累计约 1660px，页面持续运行 |
| 桌面滚轮 | **未通过**，人工操作后 wheel=0；备用按钮不算原滚轮要求通过 |
| 双屏显示 | **短时通过**，横屏和竖屏独立实例持续绘制；尚非多屏长时验收 |
| WE 官方暂停/继续 | **默认 paused-loop 短时通过**，约 12.1s 内两屏 t/draws 完全不变，继续后恢复 |
| 空闲性能 | **不完整**，602.85s 采样中进程曾停止约一分钟并重启，不能把低占用作为成绩 |
| 活跃性能 | **参考数据**：599.17s，CPU 0.078%、GPU 有效样本均值 2.75%、工作集 367MB、约28.22 draw/s；差0.83s且一个GPU样本缺失，未标记正式通过 |
| 超新星峰值 | 64.00s 有效短测：CPU 0.073%、GPU均值4.75%/最大5.31%、工作集376MB |
| 双屏短测开销 | 38.36s：CPU0.104%、GPU均值5.85%/最大8.73%、工作集734MB；不是同时间点严格对照 |

GPU 数值是目标进程的 GPU Engine 利用率求和，不等于任务管理器整卡百分比。CPU/工作集包含全部 webwallpaper64 与 wallpaperui，宿主另记。长时样本使用新增按钮前的冻结 bundle；新版按钮和下午的视觉迭代只做了浏览器验证，尚未在 WE 桌面上复测性能，不能混称最终包长时验收通过。

横屏实际分辨率为 **2560×1440**，页面 CSS1280×720、CEF DPR2、程序 framebuffer1920×1080；与原计划2560×1600不同，未修改用户分辨率。完整方法和哈希见源项目 docs/HOST-RESULTS-20260919.md。

## 尚待完成

1. 桌面滚轮路径；若最终改为按钮方案，需要用户明确接受对原要求的调整。
2. 备用按钮在真实桌面上的点击确认、竖屏输入。
3. 指定分辨率下连续、足时且不中断的空闲/活跃性能验收与多屏长时测试。
4. wall-clock恢复、锁屏/休眠等实际宿主场景。
5. 用户视觉终审、最新官方同人政策复核与发布许可。
6. 源码暂存/提交或独立仓库备份。

## 恢复与隐私

本次得到用户允许后临时切换壁纸及最大化窗口播放规则。结束后已核对 **三条壁纸记录与九项播放设置全部恢复**，测试 CEF 进程数0，接收器停止。没有隐藏图标、修改系统分辨率或上传内容。

个人机器路径、原始日志和恢复快照只在忽略的 .desktop-test/ 中，不进入交付包。PLAN.md保留历史设计，若与旧勾选冲突，以本状态和最新实测为准。

